---
# tasks file for ansible-medrecord
- apt: name=git state=present
- git: repo={{ medrecord_repo }} dest={{ medrecord_base }}
- lineinfile: dest=/opt/medrecord/java/gradlew regexp=^DEFAULT_JVM_OPTS= line=DEFAULT_JVM_OPTS="-Xms128m -Xmx512m"
